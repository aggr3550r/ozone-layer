export enum ServiceType {
  VERIFICATION_PROVIDER_SERVICE = 'verification.provider.service',
  VERIFICATION_SERVICE_SERVICE = 'verification.service.service',
  VERIFICATION_SERVICE_CONFIG_SERVICE = 'verification.service.config.service',
}
